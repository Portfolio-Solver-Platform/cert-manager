apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: monitoring-cert
  namespace: {{ .Values.gateway.namespace }}
spec:
  secretName: {{ .Values.certificates.monitoring.secretName }}
  issuerRef:
    name: {{ .Values.issuer.name }}
    kind: ClusterIssuer
  commonName: {{ .Values.certificates.monitoring.domainName }}
  dnsNames:
  - {{ .Values.certificates.monitoring.domainName }}
