apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: secrets-cert
  namespace: {{ .Values.gateway.namespace }}
spec:
  secretName: {{ .Values.certificates.secrets.secretName }}
  issuerRef:
    name: {{ .Values.issuer.name }}
    kind: ClusterIssuer
  commonName: {{ .Values.certificates.secrets.domainName }}
  dnsNames:
  - {{ .Values.certificates.secrets.domainName }}
